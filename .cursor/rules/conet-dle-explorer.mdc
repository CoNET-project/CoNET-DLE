---
description: DLE Explorer Home：Clusters，禁止 Tip height 面板
globs: explorer/**
alwaysApply: false
---

# Explorer

见 `explorer/RULES.md`。Home 第一格 **Chain ID** 只显示 `0x44c45`（hint **CoNET-DLE Testnet**），其下 **Group ID** 胶囊 = L1 bootstrap `registerTxHash`（点击打开 Blockscout `/tx/…`），禁止 `Decimal 281,669. Not CoNET L1 224422.`。第二格是 **Clusters**（\(G_e\)，无裂变 = 1；实验室 M6 裂变 = 2），不是 Tip height。第二组用户可见 ID = L1 `registerLiveGroup` tx `0xf781f2c2…876d5153`（主机已 emit 为 `hop1.ownGroupId` / `liveGroupIds`），**必须**链到 Blockscout `/tx/`；实验室 keccak / uint `2` 经 `canonicalGroupId` 别名到该 tx。保留 Tip hash 行（DLE `/hash/`）。解析 `/health.liveGroupIds` 须先 `canonicalGroupId` 再计数，避免旧归档 v1 与 hash、或 G2 keccak 与登记 tx 被当成两组。`/hash/:hash` M7 pills：`tipStateRoot` / `membershipRoot` 为一等 kind，不是 AC。Certificates 可展示 P6 `newchainArchivePending` / `newchainArchiveCertified` / `newchainValidatorQuorumEip712`；live \(Q_V\) 是实验室 EIP-712 `ArchiveValidatorQuorumAttest`（**P18**），不是生产 secp256k1。On-demand beacon 是先冻后绑实验室 keccak（**P19**），不是 live CL RANDAO；不得把 `ondemandLabBeaconAfterFreeze` 画成生产信标。等待钩不 intra-group gossip（**P20**）；实验室 HTTP 不是生产 DePIN gossip；不得把 `ondemandHookNotGossip` 画成生产 gossip。新链 AC 不得改 NFT 42 tip。
